import { PortalShell } from "@/components/layout/portal-shell";

export default function CurriculumEvaluatorLayout({ children }: { children: React.ReactNode }) {
  return <PortalShell role="curriculum-evaluator">{children}</PortalShell>;
}
